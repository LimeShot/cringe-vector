<Window x:Name="MainWindowElement"
        x:Class="CringeCraft.Client.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:glWpfControl="clr-namespace:OpenTK.Wpf;assembly=GLWpfControl"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:CringeCraft.Client.View"
        mc:Ignorable="d"
        Title="Владычество кринжа 1: Наследие позора"
        Height="450"
        Width="800">

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Файл">
                <MenuItem Header="_Создать">
                    <MenuItem.Icon>
                        <Image Source="pack://siteoforigin:,,,/assets/icons/new.png"
                               Width="16"
                               Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="_Сохранить"
                          Command="{Binding SaveFileCommand}">
                    <MenuItem.Icon>
                        <Image Source="pack://siteoforigin:,,,/assets/icons/save.png"
                               Width="16"
                               Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="_Открыть"
                          Command="{Binding OpenFileCommand}">
                    <MenuItem.Icon>
                        <Image Source="pack://siteoforigin:,,,/assets/icons/open.png"
                               Width="16"
                               Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
        </Menu>

        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="Холст: 500x500"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="Нет выделения"/>
            </StatusBarItem>
        </StatusBar>

        <Grid x:Name="MainGrid">
            <glWpfControl:GLWpfControl x:Name="OpenTkControl">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseDown">
                        <i:InvokeCommandAction Command="{Binding MouseDownCommand}"
                                               PassEventArgsToCommand="True"/>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseMove">
                        <i:InvokeCommandAction Command="{Binding MouseMoveCommand}"
                                               PassEventArgsToCommand="True"/>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseUp">
                        <i:InvokeCommandAction Command="{Binding MouseUpCommand}"
                                               PassEventArgsToCommand="True"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </glWpfControl:GLWpfControl>

            <Border
                BorderThickness="1"
                CornerRadius="2"
                Background="#ccefefef"
                Width="75"
                Margin="5"
                Padding="5"
                HorizontalAlignment="Left"
                VerticalAlignment="Top">

                <Border.Effect>
                    <DropShadowEffect ShadowDepth="3"
                                      Opacity="0.5"/>
                </Border.Effect>

                <WrapPanel
                    x:Name="ToolsPanel"
                    HorizontalAlignment="Center">
                </WrapPanel>
            </Border>
            
            <Border
                BorderThickness="1"
                CornerRadius="2"
                Background="#ccefefef"
                Width="150"
                Margin="5"
                Padding="5"
                HorizontalAlignment="Right"
                VerticalAlignment="Top">

                <Border.Effect>
                    <DropShadowEffect ShadowDepth="3"
                                      Opacity="0.5"/>
                </Border.Effect>

                <WrapPanel
                    x:Name="LayersPanel"
                    HorizontalAlignment="Center"
                    Orientation="Vertical">

                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Image Source="pack://siteoforigin:,,,/assets/tools/line.png" Width="20" Height="20" Margin="5,0,5,0"/>
                        <StackPanel>
                            <TextBlock Text="Линия" VerticalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ToggleButton x:Name="LineVisibilityToggle" IsChecked="True" VerticalAlignment="Center">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <Image Source="pack://siteoforigin:,,,/assets/icons/view.png" Width="16" Height="16"/>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsChecked" Value="False">
                                                    <Setter Property="Content">
                                                        <Setter.Value>
                                                            <Image Source="pack://siteoforigin:,,,/assets/icons/hide.png" Width="16" Height="16"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                                <CheckBox Content="Обводка" FontSize="10" Margin="5,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Image Source="pack://siteoforigin:,,,/assets/tools/rectangle.png" Width="20" Height="20" Margin="5,0,5,0"/>
                        <StackPanel>
                            <TextBlock Text="Прямоугольник" VerticalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ToggleButton x:Name="RectangleVisibilityToggle" IsChecked="True" VerticalAlignment="Center">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <Image Source="pack://siteoforigin:,,,/assets/icons/view.png" Width="16" Height="16"/>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsChecked" Value="False">
                                                    <Setter Property="Content">
                                                        <Setter.Value>
                                                            <Image Source="pack://siteoforigin:,,,/assets/icons/hide.png" Width="16" Height="16"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                                <CheckBox Content="Обводка" FontSize="10" Margin="5,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Image Source="pack://siteoforigin:,,,/assets/tools/ellipse.png" Width="20" Height="20" Margin="5,0,5,0"/>
                        <StackPanel>
                            <TextBlock Text="Прямоугольник" VerticalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ToggleButton x:Name="EllipseVisibilityToggle" IsChecked="True" VerticalAlignment="Center">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <Image Source="pack://siteoforigin:,,,/assets/icons/view.png" Width="16" Height="16"/>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsChecked" Value="False">
                                                    <Setter Property="Content">
                                                        <Setter.Value>
                                                            <Image Source="pack://siteoforigin:,,,/assets/icons/hide.png" Width="16" Height="16"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                                <CheckBox Content="Обводка" FontSize="10" Margin="5,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </WrapPanel>
            </Border>
        </Grid>
    </DockPanel>
</Window>
